# Lab 04 - Task 1 (RegistrationForm)

I built a registration form using controlled inputs (value + onChange).
On every input change I validate the value and show an error message under the field.
On submit I call preventDefault(), validate all fields again, and show success message if everything is valid.
